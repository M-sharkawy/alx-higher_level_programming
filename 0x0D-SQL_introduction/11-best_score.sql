-- lists all records with a score >= 10 in the table second_table
-- display both the score and the name and should be ordered by score (top first)
SELECT score, name WHERE score >= 10 ORDER BY score DESC;
