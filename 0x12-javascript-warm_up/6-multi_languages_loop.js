#!/usr/bin/node

const arr_strings = ["C is fun", "Python is cool", "JavaScript is amazing"]
const length_ = arr_strings.length
let ind = 0;

for (ind; ind < length_; ind++)
{
    console.log(arr_strings[ind]);
}
